# Repository Guidelines

## Project Overview
Single-module Android app (Kotlin, Jetpack Compose + Material3) for browsing local images/videos via Storage Access Framework (SAF).
- **License:** AGPL-3.0-only
- **Current version:** beta0.7.6 (versionCode 50)
- **No ViewModel, no DI, no nav component** — all state lives as `remember { mutableStateOf(...) }` inside `MainApp` composable (`MainActivity.kt:98`). Screen routing is an `if/else` on an `isViewing` boolean.
- **No media permissions in manifest** — SAF-only file access. No `READ_EXTERNAL_STORAGE`, `READ_MEDIA_IMAGES`, or `READ_MEDIA_VIDEO`.
- **Material You:** dynamic colors on Android 12+ (`dynamicDarkColorScheme`/`dynamicLightColorScheme`), manual fallback on older versions.

## Architecture & Data Flow
`folder URIs (SAF)` → `MediaScanner` recursion on `Dispatchers.IO` → results serialized to JSON file via `android.util.JsonReader`/`JsonWriter` + metadata in `SharedPreferences` → shuffled for random browse → `MediaViewerScreen`.
- Two screens: `FolderPickerScreen` (home) and `MediaViewerScreen` (viewer).
- `isFavoriteBrowsing` boolean controls favorites-only mode.
- Cache IO on `Dispatchers.IO`; Compose state via `LaunchedEffect`/`DisposableEffect`.
- Videos use Media3/ExoPlayer in `AndroidView` wrapping `PlayerView`. Import alias: `import androidx.media3.common.MediaItem as ExoMediaItem` (avoids name clash with domain `MediaItem`).

## Key Source Files
All under `android/app/src/main/java/com/jp/app/`:
- `MainActivity.kt` — app hub: state, screen routing, cache persistence, theme.
- `data/MediaScanner.kt` — recursive SAF directory scanner with progress callbacks.
- `data/Models.kt` — `MediaItem(uri, name, mimeType, size, folderUri, modifiedAt)` and `AppSettings`.
- `ui/FolderPickerScreen.kt` — folder management, scan status, cache controls, about dialog.
- `ui/MediaViewerScreen.kt` — full-screen viewer: swipe, pinch-zoom, image (Coil), video (ExoPlayer).

## Build & Verification
- **Do NOT run local Gradle builds during agent development.** Use GitHub Actions for verification (`.github/workflows/build.yml`).
- Local build if human-requested: `./gradlew assembleDebug` from `android/` directory.
- **No test suite exists.** CI build is the only verification. Gesture/UI changes need manual device testing.
- Debug APK: `app/build/outputs/apk/debug/jp-media-viewer-{versionName}-debug.apk`

## Release Workflow
After every feature/fix, create a full release:
1. PR merges to `main` (verify CI passes first).
2. Bump `versionCode` (+1) and `versionName` in `android/app/build.gradle.kts` and this file.
3. Update `.github/release-notes.md` with changelog.
4. Commit, tag (`git tag betaX.Y.Z`), push commit then tag separately: `git push origin main && git push origin betaX.Y.Z`.
5. Confirm the tag-triggered CI run started (it builds signed release APK + creates GitHub Release automatically). Do not wait for completion unless asked.
- CI reads release body from `.github/release-notes.md`.

## Commit Convention
```
type(scope): 主题

正文（中文）

由AI生成
Generated by AI
```
Subject and body in Chinese. Footer always `由AI生成` then `Generated by AI`.

## Known Risks & Maintenance Notes

### R8 混淆 (release 构建)
- `proguard-rules.pro` 已覆盖 Compose runtime、Coil、Media3、data 模型、DocumentsContract。
- 项目使用手动 `JsonReader`/`JsonWriter` 做 JSON 序列化，不依赖类名反射，R8 安全。
- **注意**：添加新第三方库时检查是否需要额外 keep 规则，否则 release APK 可能出现 `ClassNotFoundException`。
- R8 只影响 release 构建，debug 不受影响。

### SAF 扫描器 (直接 ContentResolver 查询)
- 使用 `DocumentsContract.buildChildDocumentsUriUsingTree()` + `ContentResolver.query()`，API 21+，minSdk=26 完全兼容。
- `.nomedia` 检测通过 children 列表中 `displayName == ".nomedia"` 判断，与之前 `DocumentFile.findFile()` 行为等价。
- 部分特殊存储提供器（如国产网盘 DocumentProvider）可能对 projection 列支持不完整，会导致该目录被标记为 `failedDir`。这是提供器自身的局限，非本扫描器的 bug。
- 已移除 `androidx.documentfile:documentfile` 依赖，如需使用 `DocumentFile` API 须重新添加。

### ExoPlayer 实例复用
- 单实例通过 `player.setMediaItem()` 切换媒体，不再按 URI 重建。
- 生命周期感知使用 `androidx.compose.ui.platform.LocalLifecycleOwner`（非 `lifecycle.compose` 包，后者需额外依赖）。
- 如果未来升级 Compose BOM 到 2024.06+，`LocalLifecycleOwner` 会被标记 deprecated，届时迁移到 `androidx.lifecycle.compose.LocalLifecycleOwner` 并添加 `lifecycle-runtime-compose` 依赖。

## Code Review Findings (2025-06-24)

### 2. `MediaCache.kt` 中存在死代码 `shouldSavePartialCache` 函数
- `MediaCache.kt:109-118` 有一个公开函数 `shouldSavePartialCache(currentItemCount, lastSavedItemCount, lastSavedAt, now)`，但 **没有任何地方调用它**。
- `MainActivity.kt:287` 中有自己的局部变量同名，是内联写的逻辑。
- 这个公开函数是死代码，应该删除。

### 3. `SubfolderSortMode` 从 `private` 改为 `public` — 不必要
- `MainActivity.kt:49` 中 `SubfolderSortMode` 是 `public enum`，但没有任何外部文件引用它。
- `SubfolderSorting.kt` 通过包内可见性访问，不需要 public。
- 建议改回 `private`。

### 4. 视频播放器提交链存在大量反复修复 — 质量信号不佳
从 `8b55230` 到 `5799818` 共 6 次提交都在修同一个 `VideoPlayer.kt` 的问题：

```
8b55230 feat(video): Add ExoPlayer controller and video status indicator
cf15cb0 fix(video): Fix compilation errors in video player controller
d900375 fix(video): Replace LaunchedEffect with DisposableEffect for onDispose
0633405 fix(video): Return DisposableEffectResult to fix compilation error
c09d231 fix(video): Reconstruct complete file with correct DisposableEffect pattern
2a3a8ee fix: 修复双Agent重构导致的编译错误
8a52a82 feat(video): 添加视频静音切换按钮
5799818 fix: VideoPlayer.kt 缺少 LaunchedEffect import
```

这 7 次提交中只有 2 次是功能提交，其余 5 次都是修复编译错误。尤其是最后一次 `5799818` 只加了一行 import，说明前面的重构/修改没有经过基本的编译验证就提交了。

### 5. `VideoPlayer.kt` 中 `LaunchedEffect(player)` 的使用值得注意
- `VideoPlayer.kt:40` 使用了 `LaunchedEffect(player)`，其中 `player` 是通过 `remember` 创建的 `ExoPlayer` 实例。
- 由于 `remember` 保证同一 Composition 期间返回相同实例，这个 `LaunchedEffect` 实际上只会执行一次。
- 这本身不是 bug，但如果未来 `remember` 的 key 变化，可能会产生意外行为。可以考虑改用 `LaunchedEffect(Unit)` 或 `DisposableEffect` 来表达"只执行一次"的意图。

## Media Model
```kotlin
data class MediaItem(
    val uri: Uri, val name: String, val mimeType: String,
    val size: Long, val folderUri: Uri, val modifiedAt: Long
) {
    val isVideo: Boolean get() = mimeType.startsWith("video/")
    val isImage: Boolean get() = mimeType.startsWith("image/")
}

## Karpathy Coding Guidelines

基于 Andrej Karpathy 的 LLM 编码陷阱观察的编码行为指南。适用于所有非特定领域任务，确保代码简洁、精准、可验证。

### 1. Think Before Coding

**不要假设。不要隐藏困惑。呈现权衡。**

在实现之前：
- 明确陈述你的假设。如果不确定，直接问。
- 如果存在多个解释，呈现它们——不要默默选择一个。
- 如果存在更简单的方法，指出它。在合理时提出反对意见。
- 如果某些事情不清楚，停下来。说出什么让你困惑。询问。

### 2. Simplicity First

**最小代码解决问题。没有投机性代码。**

- 不要添加用户没要求的功能。
- 不要为单次使用创建抽象。
- 不要添加用户没要求的"灵活性"或"可配置性"。
- 不要为不可能的场景添加错误处理。
- 如果你写了 200 行但可以是 50 行，重写它。

### 3. Surgical Changes

**只碰必须碰的。只清理你自己的烂摊子。**

- 不要"改进"相邻的代码、注释或格式。
- 不要重构没坏的东西。
- 匹配现有风格，即使你可能会用不同方式写。
- 如果你注意到无关的死代码，指出它——不要删除它。

### 4. Goal-Driven Execution

**定义成功标准。循环直到验证。**

- "添加验证" → "为无效输入写测试，然后让它们通过"
- "修复 bug" → "写一个复现它的测试，然后让它通过"
- "重构 X" → "确保测试在重构前后都通过"

---
```
