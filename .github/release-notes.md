APK 自动生成版本 beta0.7.6，主要更新为视频播放控制增强。

### 新功能
- **ExoPlayer 控制条**：视频播放器启用 `useController = true`，支持播放/暂停、进度切换、音量调节、拖动
- **控制条自动隐藏**：ExoPlayer 内置控制条 3 秒无操作自动隐藏
- **视频状态指示器**：底部状态栏显示播放/暂停、静音/非静音状态图标

### 修改
- 移除了未使用的 `videoPlaybackState` 变量和 `VolumeOff` 图标

### 注意
- 本版本不需要媒体权限，只允许用户通过系统文件选择器授权的目录
