APK 自动构建版本 beta0.7.3。本次发布优化列表计算性能。

### 优化内容
- **derivedStateOf 优化**：`visibleItems` 列表计算使用 `derivedStateOf` 包装，仅在依赖状态真正变化时重新计算
- **减少无效重组**：避免手势动画期间每帧都重新过滤/排序整个媒体列表

### 预期效果
- 滑动浏览时更流畅（不再每帧 O(n log n) 排序）
- 大媒体库（10k+ 文件）下显著减少卡顿

### 注意
- 本版本不新增媒体权限，仍只访问用户通过系统文件夹选择器授权的目录。
