APK 自动构建版本 beta0.0.7。此版本为 debug 签名，可直接安装用于测试。

### 变更内容
- 输出命名：APK 文件名改为包含版本号和构建类型，便于区分下载包
- 版本元数据：应用版本更新为 beta0.0.7
- CI 发布：Actions artifact 和 Release 附件匹配新的版本化 APK 文件名
- 媒体缓存：缓存读写迁移到后台线程，并在缓存恢复前校验文件可访问性
- 外部链接：关于页外部链接失败时会显示提示，不再静默吞异常
- 文件夹授权：选择文件夹仅在持久化授权成功后才加入列表
