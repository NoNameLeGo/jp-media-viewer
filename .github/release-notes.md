APK 自动构建版本 beta0.1.5-cache-test。此版本保留 beta0.1.3 扫描统计详情回退，并修复重新打开应用后缓存命中仍像重新扫描的问题。

### 修复内容
- 回退扫描统计详情：移除 beta0.1.3 引入的每文件夹统计、失败目录统计、扫描耗时统计和对应首页详情展示
- 媒体缓存：启动时命中缓存后直接使用缓存媒体列表，不再逐个调用 SAF `exists()` 校验；文件变化由浏览失败提示和手动“重新扫描”处理
- 版本元数据：应用版本更新为 beta0.1.5-cache-test，versionCode 更新为 19

### 版本信息
- Release APK 继续使用稳定签名，支持从 beta0.0.8 及之后 release 签名版本覆盖安装

### 注意
- 如果设备上安装的是 beta0.0.7 或更早 debug 签名版本，仍需先卸载旧版再安装 release 签名 APK。
