# 待修复问题与功能建议

## 已修复

- 扫描中先行浏览

   扫描过程中即可点击"开始浏览"查看已扫描到的媒体，扫描在后台继续进行；浏览期间新增的媒体会在扫描完成后自动加入列表并随机排序。


1. 优化媒体缓存存储方式

   已将缓存读写移到 `Dispatchers.IO`，并在缓存命中前做媒体可访问性校验，避免主线程阻塞和失效缓存直接进入浏览。

2. 为外部链接打开增加兜底

   关于页打开项目地址和问题反馈时已改为 `runCatching` 包裹，并在失败时显示提示，不再静默吞掉异常。

3. 缓存命中后处理文件失效

   已在缓存恢复后校验文件是否仍可访问；如果缓存失效，会清理缓存并重新扫描。

4. 调整文件夹授权处理

   选择文件夹时只持久化 SAF 读取权限，避免请求未授予的写入权限导致授权失败；只有在 `takePersistableUriPermission(...)` 成功后才会加入列表，失败时会提示授权失败。

5. 调整备份策略

   `AndroidManifest.xml` 保持 `android:allowBackup="false"`，并增加 `backup_rules.xml` 与 `data_extraction_rules.xml`，显式排除保存文件夹授权 URI、收藏 URI 和媒体缓存的 `settings` 偏好文件。

6. 精简 SAF 场景下的媒体读取权限

   已确认当前 manifest 没有 `READ_EXTERNAL_STORAGE`、`READ_MEDIA_IMAGES`、`READ_MEDIA_VIDEO` 等媒体权限；应用继续只依赖 Storage Access Framework 的文件夹授权。

7. 改进发布说明维护方式

   GitHub Actions 已改为从 `.github/release-notes.md` 读取 release notes，避免在 workflow 中维护大段硬编码说明。

8. 增加媒体加载失败提示

   图片解码失败或视频播放失败时会显示“媒体读取失败”提示，并提供“重新扫描”操作刷新媒体列表。

9. 正式发布签名

   Actions 已改为从 GitHub Secrets 还原 release keystore，并在 tag 发布时构建签名 release APK。beta0.0.8 是第一版稳定 release 签名 APK；从该版本之后，同签名 release APK 可以直接覆盖安装。

10. 复制文件夹名称

   浏览页底部“文件夹”信息支持点击复制当前媒体所在文件夹名称，并在复制成功后显示提示。

11. 优化浏览控制层动画

   浏览页点击显示控制信息时，顶部工具栏从上方出现，底部文件信息从下方出现，避免所有信息都从顶部滑下。

12. 缓存管理

   首页显示媒体缓存大小和收藏数量，支持清除媒体缓存和一键清空收藏。

13. 扫描统计详情

   首页显示扫描耗时、检查文件数、找到媒体数、跳过 `.nomedia` 目录数、失败目录数，以及每个文件夹的扫描结果。

14. 图片缩放和平移

   图片浏览支持双指缩放，放大后可拖动平移查看细节；放大状态下禁用上下滑切换，避免手势冲突。

## 建议后续修复

暂无。

## 功能推荐

1. 排序和随机模式切换

   支持随机、按文件名、按修改时间、新到旧、旧到新等浏览顺序。

2. 收藏管理增强

   收藏浏览时增加明确的移除按钮、批量清空、失效收藏清理。

3. 当前文件操作

   提供分享当前图片/视频、复制文件名、复制 URI、尝试用系统应用打开。

4. 多文件夹分组浏览

   支持只浏览某一个已添加文件夹，或浏览全部文件夹。

5. 视频播放控制

   增加暂停/播放、进度条、静音、倍速等基础控制。
