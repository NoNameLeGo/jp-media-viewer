# 待修复问题与功能建议

## 建议优先修复

1. 优化媒体缓存存储方式

   当前完整媒体列表通过 `SharedPreferences` 保存为 JSON。媒体数量较大时，启动解析和扫描后序列化可能阻塞主线程。短期建议把缓存读写移到 `Dispatchers.IO`；长期建议改用 Room 或独立 JSON 文件缓存。

2. 调整备份策略

   `AndroidManifest.xml` 当前允许备份应用数据。文件夹授权 URI、收藏 URI 和媒体缓存恢复到新设备后可能失效，也会把本地媒体路径类信息纳入备份范围。建议关闭备份，或添加 `dataExtractionRules` 排除相关 `SharedPreferences`。

3. 精简 SAF 场景下的媒体读取权限

   应用主要通过 Storage Access Framework 的文件夹授权读取媒体，通常不需要 `READ_EXTERNAL_STORAGE`、`READ_MEDIA_IMAGES`、`READ_MEDIA_VIDEO`。建议确认没有 MediaStore 全盘扫描需求后移除这些权限，降低权限面。

4. 为外部链接打开增加兜底

   关于页打开项目地址和问题反馈时直接调用 `startActivity`。极端情况下如果没有可处理浏览器，可能抛出 `ActivityNotFoundException`。建议用 `runCatching` 包裹并显示失败提示。

5. 缓存命中后处理文件失效

   当前缓存命中后直接恢复媒体列表，不验证文件是否仍可访问。建议在图片/视频加载失败时提示“文件不可访问，请重新扫描”，或在进入浏览前做轻量抽样验证。

6. 改进发布说明维护方式

   GitHub Actions 里的 Release notes 仍是硬编码文本。建议后续改为从 `CHANGELOG.md` 或版本片段读取，避免发新版 tag 时沿用旧内容。

## 功能推荐

1. 媒体加载失败提示

   当缓存中的文件已删除、权限失效、图片解码失败或视频播放失败时，显示明确提示，并提供“重新扫描”。

2. 排序和随机模式切换

   支持随机、按文件名、按修改时间、新到旧、旧到新等浏览顺序。

3. 图片缩放和平移

   支持双指缩放、拖动查看大图细节。

4. 收藏管理增强

   收藏浏览时增加明确的移除按钮、批量清空、失效收藏清理。

5. 当前文件操作

   提供分享当前图片/视频、复制文件名、复制 URI、尝试用系统应用打开等操作。

6. 扫描统计详情

   显示每个文件夹找到多少媒体、跳过多少 `.nomedia` 目录、扫描耗时和失败目录。

7. 缓存管理

   设置页增加清除媒体缓存、清除收藏、查看缓存大小。

8. 多文件夹分组浏览

   支持只浏览某一个已添加文件夹，或浏览全部文件夹。

9. 视频播放控制

   增加暂停/播放、进度条、静音、倍速等基础控制。

10. 正式发布签名

    目前自动发布的是 debug APK。公开分发前建议增加正式 keystore 签名、release APK/AAB 和版本 changelog。
